package org.ilrt.mca.rdf;

/**
 * A simple interface that returns <code>StoreWrapper</code> objects.
 *
 * @author Mike Jones (mike.a.jones@bristol.ac.uk)
 */
public interface StoreWrapperManager {

    StoreWrapper getStoreWrapper();
}
